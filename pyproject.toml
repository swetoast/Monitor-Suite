[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "monitor-suite-agent"
version = "2.8.4"
description = "Trusted-LAN Linux telemetry agent for Monitor Suite"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn>=0.34,<1.0",
]

[project.scripts]
monitor-suite-agent = "monitor_suite_agent.__main__:main"
monitor-suite-smart-collector = "monitor_suite_agent.smart_collector:main"
monitor-suite-power-collector = "monitor_suite_agent.power_collector:main"

[tool.setuptools]
packages = ["monitor_suite_agent"]

[tool.pytest.ini_options]
testpaths = ["tests"]
